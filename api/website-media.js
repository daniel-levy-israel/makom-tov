const {getProperty}=require('../lib/catalog');const {fetchOfficialWebsite}=require('../lib/website-media');
module.exports=async function(req,res){if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'method_not_allowed'})}if(!process.env.WEBSITE_MEDIA_SIGNING_SECRET)return res.status(404).json({error:'website_media_not_configured'});try{let p=await getProperty(req.query.id);if(!p?.website)return res.status(404).json({error:'official_website_missing'});let data=await fetchOfficialWebsite(p);if(!data?.images.length)return res.status(404).json({error:'official_images_not_found'});res.setHeader('Cache-Control','private, max-age=900');return res.status(200).json({...data,provider:'Official website'})}catch(e){console.error('website_media_failed',e);return res.status(502).json({error:'website_media_failed'})}}
